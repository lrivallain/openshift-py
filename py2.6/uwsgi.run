#!/bin/bash

# Activate the venv
. /opt/openshift/python/virtenv/bin/activate

# install project deps
cd /data/ && python setup.py install

# run project
cd wsgi/  && uwsgi --http :8080 --wsgi-file application
